# 260911 — Handoff: state of the HardDisks validation (COWORK → next session)

Read this first in a fresh session. Doctrine: shared memory holds the map, the disk holds the territory — everything below is on disk under `0000_PLAN_OVERALL/ALL_MARKDOWNS/` (notes, YYMMDD_ prefixes) and the campaign folders named here. Rules for every Claude Code (CC) prompt: no core physics changes, no edits to accepted trajectories, no commits without Chris's go, quote numbers from files, bit-identity gates are pass/fail. Chris pastes only CC's final report (not the transcript) to keep context small. Explanations for Chris: step by step, formulas in LaTeX, PDFs he can read on the tablet (three exist: `260910_hand_calculations_*`, `260910_ELI5_plan_*`, `260911_chi2_and_box_size_*`). Do not create files without announcing them.

## Paper 1 — equilibrium validation

**Pressure (done, claim text being written).** Campaign `hspist3/experiments_speed_of_sound/EDMD/mode1_normalized_units/00_pressure_validation_20260907/` (+ `N2500_20260910/`). Z_pair and Z_wall agree by momentum balance; strict health contract; 1/√N extrapolation with χ². Claim range: **η ≤ 0.65 validated** (±0.17 % vs Kolafa–Rottner for 0.2–0.5, +0.13 % at 0.60, −0.35 ± 0.06 % at 0.65 with four sizes 400/900/1600/2500, χ²₂ = 1.17); **0.67–0.69 no bulk value** (χ²₂ = 34 and 16, non-monotone in N); ≥ 0.70 exploratory; η = 0.30 trips χ² = 4.05 (p = 0.044), reported as the expected one false alarm in twelve tests. Pressure runner uses measured KE and its seeders do no drift removal → immune to the seeding bias below. Document: `260908_pressure_final_analysis.md` (regenerated by `hspist3/validation/write_pressure_final_analysis.py`).

**Speed of sound, route A (instrument validated; bulk claim pending famB).** Campaign `.../00_eta_sweep_ROMAN/campaign_r25_psi6_20260823/` : N = 100 (50 per side), r = 0.5, H = 10, L0 = 3.927/η, 32 η from 0.0196 to 0.76, 9 divider masses (50…2000), 25 repeats, ≥ 25 oscillations per run (7200 trajectories, 7186 eligible). Román relation ν = c_s K/(2π L_eff), cot K = (M/2Nm)K, L_eff = L0 − 2r; fit through the origin. Refit table `ALL_MARKDOWNS/260909_plots/routeA_refit_cs_vs_eta_20260909.csv`; figures `260909_plots/260910_FINAL_cs_vs_eta.pdf` and `_cs_idealgas_zoom.pdf` (script `plot_cs_final_style.py`, old FINAL style: shaded regimes, SPT, Henderson, KR main reference, Román 2002 points, no Liu). Route A keeps Kn = λ/L0 ≈ 0.14 and ωτ_c ≈ 0.1 fixed at every η (λ = 0.555 σ/η dilute) → it is a confinement sweep, not a bulk measurement.

**Seeding bias found 2026-09-11 (CC item 1), affects all speed-of-sound and energy-transfer runs, not pressure.** `equalize_temperature_per_segment()` rescales each compartment to kT = 1 and *then* removes its centre-of-mass velocity, which costs ⟨½N m v_cm²⟩ = kT per compartment: T_i = 1 − X/50 with X ~ Exp(1) (measured mean 0.980, sd 0.020, 7200/7200 reconstructed from seed; columns T_i_left/right/mean now in `routeA_fit_input_manifest_20260909.csv`). Correcting ν by √T_i moves every c_s up ≈ 1 %: dilute end goes from −0.2 % to **+0.8 % vs KR**, η = 0.39 from +1.1 % to +2.2 %. Cowork's reading: the +0.8 % is the finite-Knudsen (ωτ_c ≈ 0.1) dispersion offset of a fixed-Kn sweep, i.e. a prediction: it must shrink in famB (larger N at fixed η) and grow in the N_side = 25/10 pilot. Not yet tested. Per-run ν is FFT-bin-limited (bin = 3.2 % of ν) — that is the 0.5–0.7 % per-mass scatter; CC item 1 (running) replaces peak-picking with a damped-cosine fit and redoes the √T_i slope test (expect 1.0 ± 0.1). New driver flag `--seed-drift-order=drift-first` (default `old`, so accepted cells stay bit-identical); all new campaigns use `drift-first` and log T_i.

**famA/famB.** famA (`finitesize_aspect_20260826/refit_famA_20260910/famA_tables.md`) covers only η = 0.67–0.73; only 0.670 extrapolates (c∞ = 10.65 ± 0.04, −6.4 % vs KR); rest rejected. **famB launched 2026-09-11 (CC item 4)**: η = 0.10, 0.30, 0.50, 0.60, 0.65; N = 100/400/900/1600 at the famA aspect ratio; 5 masses; 10 repeats; drift-first; T_i logged. This is the bulk c_s claim: per η, c_s(N) = c∞ + a/√N with χ², compared with KR.

Open (Paper 1): 504 `wall_overdue` flags resolved (two disks seeded on the wall face at L0 ≥ 100, float pad; harmless, kept) — the float seed pad must be fixed before any new run at L0 ≥ 100. Route A extension to η = 0.01/0.005 and the Knudsen pilot (N_side = 25, 10 at L0 = 200; expect c_s rising from √2 toward √3) are queued as positive controls.

## Paper 2 — piston energy transfer

Reference set `hspist3/experiments_energy_transfer/mass_sweep_eta02_N600/` was run on `--edmd-acc=1` (failed validation) without gas-KE logging → not physics; superseded. Level 0 now closes on the validated core (`260910_paper2_level0_energy_ledger_CC.md`, CC's evening version): trace columns `KE_gas_total/left/right, Px_gas` added (30 → 34 columns), gated per-event log `HD_PISTON_EVENTS=<path>` with kinds PL/PR/D0…/WL/WR/WB/WT (t_sigma, u_wall, v_before, v_after, dE, dp). Pilot at η = 0.1000, L0 = 39.25 (24·L0 must be an integer), H = 10, 50 per side, held divider (mass 10⁹; the M ≤ 0 branch is not reachable from the CLI), prescribed right piston u = 0.05, travel 3.93 σ, hold 200 σ-time: ledger residual, Σ dE vs PistonWork and momentum closure all at 10⁻¹¹. Two driver bugs fixed (gated bit-identical): `--wall-positions` used the default L0; energy-transfer runs overwrote `experiments_energy_transfer/00_COMMAND.md`.

Dissipation, 25 seeds per u (drift-first, T_i = 1.000000): u = 0.02/0.05/0.10 → W_diss = 0.39/0.45/0.73 ± 0.04/0.06/0.13 kT with W_qs = 7.06 T_i (KR adiabat over η 0.1000 → 0.1112, compression 10 % of length = 11.1 % in density). Fit W_diss = 4.33 u + 0.28: **the u-independent 0.28 kT is not dissipation** — suspects are W_qs computed with the bulk EOS for a 50-disk box (wall layer ≈ 25 % of area) and/or an unrelaxed lattice start (hold 16.7 σ-time < one sound crossing). CC item 2 (running): long hold, u = 0.005/0.01, extrapolate W(u → 0), and measure the compartment's own Z from piston-face events at u = 0.

Ladder after Level 0: Level 1 held-divider force autocorrelation → friction ζ (Sivak–Crooks), equipartition check; Level 2 work histograms at several u, Jarzynski with canonical starts (needs βW_diss ≲ 5, i.e. N = 50–300 per side, u/c_s = 0.01–0.3); Level 3 W_diss(u) linear with slope ζΔx; Level 4 nostalgia I(s_t;x_t) − I(s_t;x_{t+1}) ≥ 0 with a stochastic piston command, 10³–10⁴ trajectories, bound βW_diss ≥ I_nost (Still et al. 2012). Formulas: `260910_hand_calculations_cs_to_dissipation_COWORK.pdf` §7–11.

## Repo state / housekeeping
Checkpoint commit prepared by CC (explicit paths; 218 untracked paths in the tree, never `git add -A`); Chris runs it. `edmd_accelerated.{c,h}` diffs are the 2026-08-22 tunnelling port, not new. FIGURES pruned: campaign-level at top, per-η pages in `FIGURES/per_eta/`. Advisor meeting prep and terminal commands: `260911_advisor_meeting_prep_and_commands_COWORK.md`.

## What the next session does first
1. Read CC's report on items 1–4 of the 2026-09-11 prompt (frequency estimator + √T_i test; W(u → 0) and finite-box Z; pressure claim paragraph; famB launch state). Check: slope test ≈ 1; W_0 vs 7.06 T_i vs finite-box Z; claim paragraph wording.
2. When famB lands: manifest treatment, through-origin fits, 1/√N with χ² per η, compare with KR — and check whether the +0.8 % route-A offset shrinks with N (Knudsen prediction).
3. Regenerate the c_s figures from the corrected table (`plot_cs_final_style.py <csv> <prefix>`), then write the c_s claim text next to the pressure one.
4. Paper 2 Level 1 only after item 2 of the CC prompt has explained the 0.28 kT offset.
