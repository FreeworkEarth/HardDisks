# Paper 3 experiments — placeholder

Nothing is copied here yet. The existing Szilard runs are in
`hspist3/experiments_szilard/`, which is large and outside git.

When this paper starts, it gets the same layout as the other two: `final/`, `run_scripts/`, and a
README naming the canonical file.

